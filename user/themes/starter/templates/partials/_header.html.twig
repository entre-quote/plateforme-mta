{% block header %}
<header class="header">

  <div class="container">
    <a class="logo left" href="{{ base_url == '' ? '/' : base_url }}">
      <img src="{{ url('theme://images/logo-mta.svg') }}" alt="Plateforme MTA">
    </a>

    {% block header_navigation %}
        <nav class="main-nav">
          {% include 'partials/_navigation.html.twig' %}
        </nav>
    {% endblock %}

    {{page.content}}
  </div>

</header>
{% endblock %}
